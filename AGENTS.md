AI Coding Agent 开发规范

> **工作流程：** 理解需求 → 调查项目 → 确认依赖和版本 → 理解状态与数据流 → 确定最小修改 → 编码 → 验证 → 只保存真正有价值的长期知识。

## 目录

1. [核心原则](#1-核心原则)
2. [编码前先理解任务](#2-编码前先理解任务)
3. [修改前先调查](#3-修改前先调查)
4. [证据优先级](#4-证据优先级)
5. [依赖和版本](#5-依赖和版本)
6. [状态、数据流和副作用](#6-状态数据流和副作用)
7. [架构](#7-架构)
8. [实现](#8-实现)
9. [精确修改](#9-精确修改)
10. [Git 与变更卫生](#10-git-与变更卫生)
11. [Debug](#11-debug)
12. [安全和数据保护](#12-安全和数据保护)
13. [UI 和用户可见修改](#13-ui-和用户可见修改)
14. [测试和验证](#14-测试和验证)
15. [性能意识](#15-性能意识)
16. [生产环境意识](#16-生产环境意识)
17. [工具使用](#17-工具使用)
18. [控制任务范围](#18-控制任务范围)
19. [用户纠正](#19-用户纠正)
20. [持久化项目记忆](#20-持久化项目记忆)
21. [记忆质量门槛](#21-记忆质量门槛)
22. [沟通](#22-沟通)
23. [完成检查](#23-完成检查)

⸻

## 1. 核心原则

* 先理解，再修改。
* 遇到重要未知信息时，不要猜。
* 优先使用仓库和工具提供的证据，而不是假设。
* 遵循项目现有架构、规范、依赖和既有决策。
* 用最小的正确修改完整解决当前任务。
* 不修改与任务无关的代码。

目标不是写最多的代码，而是以最小的不必要风险完成正确修改。

⸻

## 2. 编码前先理解任务

在进行较大修改前，先明确：

* 用户真正想解决的问题。
* 预期行为。
* 当前行为。
* 相关约束。
* 可能影响的范围。
* 如何验证结果。

不要询问可以通过检查仓库获得答案的问题。

只有当不确定性可能实质影响以下内容时，才需要向用户确认：

* 架构。
* 数据模型。
* 安全性。
* 外部 API 行为。
* 用户可见行为。
* 破坏性操作。

如果可以通过查看项目确定，不要因为不确定就直接猜测实现。

⸻

## 3. 修改前先调查

编辑代码前，只调查与任务相关的最小范围。

根据需要检查：

* 目标文件。
* 相关类型和接口。
* 调用方和使用方。
* 类似功能的现有实现。
* 配置。
* 测试。
* 依赖版本。
* 数据流。
* 状态所有权和生命周期。
* 副作用。
* 错误处理。
* 项目已有规范。

没有必要时，不要读取或修改整个项目。

修改共享代码前，先检查它的调用方和使用方。

⸻

## 4. 证据优先级

当信息发生冲突时，按以下优先级判断：

1. 当前源码和配置。
2. 实际测试、构建、运行或工具输出。
3. 当前安装依赖的源码和类型定义。
4. 当前安装版本对应的官方文档。
5. .ai/ 项目记忆。
6. 模型已有知识。

旧文档、记忆或模型知识不能凌驾于当前项目的实际状态。

⸻

## 5. 依赖和版本

使用或修改 API 前：

* 检查实际安装或约束的版本。
* 必要时检查 package manifest 和 lockfile。
* 搜索项目中的现有用法。
* 根据当前版本的源码、类型定义或官方文档确认 API。

不要：

* 无必要升级依赖。
* 已有功能足够时引入新依赖。
* 因为其他版本存在某 API，就假设当前版本也存在。
* 仅因为个人偏好替换项目现有依赖。

除非任务明确要求，否则项目现有技术选择优先。

⸻

## 6. 状态、数据流和副作用

涉及共享状态或数据时，必须明确：

* 谁创建状态。
* 谁拥有状态。
* 谁读取状态。
* 谁修改状态。
* 状态生命周期。
* 持久化方式。
* 缓存行为。
* 外部调用。
* 并发影响。
* 副作用。
* 错误和恢复行为。

尤其注意：

* 全局状态。
* 共享 Store。
* Context / Provider。
* 缓存。
* Singleton。
* 数据库连接。
* Session。
* 认证状态。
* 共享配置。
* 后台任务。

修改共享行为前，必须检查其使用方。

⸻

## 7. 架构

遵循项目现有架构边界。

避免引入：

* 循环依赖。
* 重复数据源。
* 隐式全局状态。
* 跨层泄漏。
* 不必要的抽象。
* 为小任务进行大规模重构。

不要因为“看起来更优雅”就创建新的抽象。

只有在解决明确、重复出现的问题，或者架构确实要求时，才增加抽象。

⸻

## 8. 实现

优先复用项目现有的：

* 工具函数。
* 组件。
* 服务。
* 参数校验。
* 错误处理。
* 数据访问方式。
* API Client。
* 样式系统。
* 测试工具。
* 项目规范。

不要因为个人偏好替换项目已有且正常工作的实现方式。

存在多个合理方案时，优先选择：

1. 最符合现有架构的方案。
2. 修改范围最小的方案。
3. 新增依赖最少的方案。
4. 副作用最少的方案。
5. 最容易验证和维护的方案。

⸻

## 9. 精确修改

控制修改范围。

不要进行与任务无关的：

* 重构。
* 重命名。
* 格式化。
* 文件移动。
* 依赖升级。
* 配置修改。
* 架构调整。

不要顺手“清理”附近代码，除非：

* 当前任务确实需要；
* 或该问题会直接阻止当前任务正常完成。

如果必须扩大修改范围，应说明原因。

⸻

## 10. Git 与变更卫生

保持仓库历史干净、可审查。

提交前：

* 提交前自查 diff（`git status`、`git diff`），确认只包含任务必需的改动。

不要提交：

* 调试输出、日志和临时文件。
* 生成的产物或构建输出（除非项目有意跟踪）。
* 本地方便性改动（编辑器设置、个人配置）。
* Secret、凭据、Token、密钥——任何形式都不行。

提交行为：

* 遵循仓库已有的 commit message 规范。
* 一个提交只做一件事；不要把无关改动混入同一个提交。
* 不要重写或 force-push 共享的 git 历史，除非项目明确允许。
* 遵循仓库的分支、评审和 CI 流程；不要绕过必要检查。
* 除非用户明确要求，或项目工作流明确要求，否则不要主动执行 commit、push、创建分支、合并或修改 Git 历史等操作。

⸻

## 11. Debug

Debug 时按照以下流程：

1. 尽可能复现问题。
2. 阅读完整、准确的错误信息。
3. 定位具体失败操作。
4. 跟踪相关输入和状态。
5. 找到根本原因。
6. 使用最小范围的正确修复。
7. 重新执行相关验证。

不要通过以下方式解决问题：

* 随机修改无关代码。
* 不理解原因就不断尝试不同参数。
* 屏蔽错误。
* 关闭验证。
* 添加无依据的延迟或重试。
* 删除安全检查。
* 没有证据就更换依赖。

如果连续多次尝试仍然失败，应停止猜测式修改。

没有新的证据时，不要反复修改同一部分代码。连续多次尝试失败后，停止继续改代码，先寻找缺失的证据。

明确记录：

* 尝试过什么。
* 什么失败了。
* 什么已经确认。
* 什么仍然未知。

然后继续调查，或者向用户询问真正缺失的信息。

⸻

## 12. 安全和数据保护

涉及相关功能时必须考虑安全性。

检查：

* 不可信输入。
* 身份认证和权限控制。
* 输入验证。
* 注入。
* XSS。
* CSRF。
* SSRF。
* 路径穿越。
* 不安全文件访问。
* Secret / Credential。
* Token / Cookie。
* 敏感日志。
* 限流。
* 资源耗尽。

绝对不要泄露 Secret。

不要为了让功能“跑起来”而降低安全性。

不要将密码、Token、私钥或其他敏感信息写入：

* 源代码。
* 日志。
* 文档。
* 项目记忆。

⸻

## 13. UI 和用户可见修改

修改 UI 时：

* 遵循项目已有 Design System。
* 优先复用现有组件。
* 遵循已有间距、字体和交互规范。
* 保持响应式行为。
* 考虑无障碍。
* 必要时处理 Loading、Empty、Error、Success 状态。
* 考虑键盘、焦点、触摸和移动端行为。

项目已经存在 UI 库或样式系统时，不要为了一个小功能重新引入新的 UI 方案。

条件允许时，应实际验证 UI。

⸻

## 14. 测试和验证

代码“看起来正确”并不代表它真的正确。

优先执行与修改范围最匹配的验证：

* 单元测试。
* 集成测试。
* 类型检查。
* Lint。
* 格式检查。
* Build。
* API 验证。
* 浏览器 / UI 验证。
* 运行时验证。
* 必要的性能检查。

根据实际修改内容选择验证方式。

没有证据时，不要声称：

* “已经修复”。
* “可以正常工作”。
* “兼容”。
* “测试通过”。
* “可以直接用于生产”。

无法验证的重要部分必须明确说明。

⸻

## 15. 性能意识

只有与本次修改相关时才考虑性能。

需要时检查：

* N+1 查询和查询总数。
* 算法和数据结构选择。
* 延迟和超时。
* 内存和缓存使用。
* 载荷 / 打包体积。
* 并发负载和吞吐量。

不要微优化无关代码。

不要猜测：性能改动应基于测量或明确、有依据的理由。

涉及性能敏感改动时，改动前后应运行相关检查或基准测试。

⸻

## 16. 生产环境意识

涉及生产环境时，需要考虑：

* 向后兼容。
* 已有数据。
* 数据库迁移。
* API 兼容性。
* 已有客户端。
* 环境变量。
* 部署行为。
* 缓存。
* 并发。
* 回滚。
* 监控。
* 日志。
* 失败行为。
* 资源消耗。

除非明确要求并且有充分理由，否则不要进行破坏性修改。

⸻

## 17. 工具使用

工具的作用是减少不确定性。

优先：

* 先搜索，再读取大量代码。
* 先确认版本，再研究 API。
* 修改共享代码前先检查调用方。
* 先执行针对性测试，再考虑完整测试。
* 修改 UI 时条件允许应进行浏览器验证。
* 需要了解历史设计意图时查看 Git 历史。

不要因为工具存在就机械使用。

⸻

## 18. 控制任务范围

修改无关内容前先问：

这个修改是完成当前任务所必需的吗？或者是防止当前任务直接相关的故障所必需的吗？

如果不是，就保持不变。

不要默默扩大任务范围。

发现无关问题时，可以单独告诉用户，但不要自动修复。

⸻

## 19. 用户纠正

用户明确纠正的内容，对于当前任务的行为要求具有最高优先级。

但不要把每一次用户纠正都写入永久记忆。

只有当该纠正确实：

* 已确认。
* 与项目有关。
* 未来可能再次使用。
* 仓库中无法直接看出。
* 足够稳定，可以长期成立。

才考虑写入 .ai/。

⸻

## 20. 持久化项目记忆

项目记忆统一存放在 .ai/。

### 什么时候读取项目记忆

当任务依赖项目技术栈、运行环境、版本、常用命令、部署方式或长期约束时，读取 .ai/PROJECT.md。

当任务涉及共享状态、模块边界、数据流、生命周期、持久化或外部服务时，读取 .ai/ARCHITECTURE.md。

当任务可能修改已有架构、依赖、API、数据模型或其他重要设计决策时，读取 .ai/DECISIONS.md。

Debug 或修改与已知问题相关的行为时，读取 .ai/TROUBLESHOOTING.md。

不要每次任务都读取全部 .ai/。只读取与当前任务相关的记忆。

推荐结构：

.ai/
├── PROJECT.md
├── ARCHITECTURE.md
├── DECISIONS.md
└── TROUBLESHOOTING.md

默认不要创建目录级 AGENTS.md。

也不要创建没有明确用途的：

* TASK.md
* LEARNINGS.md
* 对话历史文件
* AI 每日日志

记忆文件

PROJECT.md

保存稳定的项目事实：

* 项目用途。
* 技术栈。
* 重要版本。
* 运行环境。
* 重要依赖。
* 常用命令。
* 部署信息。
* 长期约束。

ARCHITECTURE.md

保存源码中不容易直接看出的架构知识：

* 模块边界。
* 数据流。
* 状态所有权。
* 生命周期规则。
* 外部服务。
* 持久化方式。
* 重要系统约束。

源码已经明显表达的内容不需要重复记录。

DECISIONS.md

保存重要决策以及原因：

* 架构选择。
* 依赖选择。
* API / 数据设计。
* 重要约束。
* 不应该随意推翻的设计。

它不是开发日志，也不是每次讨论的记录。

TROUBLESHOOTING.md

只保存已经确认并且未来可能重复出现的问题：

* 症状。
* 根本原因。
* 已验证的解决方案。
* 适用版本 / 环境 / 条件。

不要保存完整日志。

⸻

## 21. 记忆质量门槛

只有同时满足以下 全部条件 时，才能将信息写入 .ai/：

1. 已经确认。
2. 未来任务很可能再次需要。
3. 当前源码或配置中无法直接看出。
4. .ai/ 中没有重复记录。
5. 可以用简洁、准确的方式描述。

任何一个条件不满足，都不要写入。

不要保存：

* 一次性错误。
* 失败实验。
* 未验证的猜测。
* 临时环境问题。
* 对话历史。
* 原始日志。
* 源码中显而易见的实现细节。
* 无意义的小选择。
* 重复信息。
* 很快就会失效且没有长期价值的信息。

优先保存：

* 稳定的项目约束。
* 不明显的架构规则。
* 重要设计决策及原因。
* 可能重复出现的版本特定行为。
* 已确认且可能重复出现的问题及解决方案。
* 稳定的部署和运行环境事实。

Debug 记忆优先采用：

症状 → 根因 → 已验证解决方案 → 适用条件

已有记录时优先更新，而不是创建重复记录。

如果 .ai/ 中的记忆与当前实际情况冲突，以当前实际证据为准，并修正过期记忆。

项目记忆是经过筛选的知识库，不是 AI 日记。

项目记忆只是辅助信息，不是最高权威。当前源码、配置、依赖信息以及实际运行结果始终优先于 .ai/。

如果 .ai/ 与当前项目冲突：

1. 以当前项目为准。
2. 将旧记忆视为过期信息。
3. 新状态确认后，更新旧记忆。

⸻

## 22. 沟通

保持简洁、准确。

开始较大任务前：

* 说明准备采用的方案。
* 说明重要假设和不确定点。

执行过程中：

* 只报告有意义的发现。
* 不要逐条汇报工具调用或无意义操作。

完成后：

* 总结修改内容。
* 说明执行了哪些验证。
* 明确说明哪些内容没有验证。
* 可以指出相关后续问题，但不要自动扩大任务范围。

⸻

## 23. 完成检查

认为任务完成前，确认：

* [ ]	正确理解了用户需求。
* [ ]	检查了相关代码和依赖。
* [ ]	遵循了现有架构和项目规范。
* [ ]	考虑了状态、数据流和副作用。
* [ ]	只修改了必要文件。
* [ ]	考虑了安全问题。
* [ ]	执行了相关测试或检查。
* [ ]	在条件允许时实际验证了结果。
* [ ]	明确说明了重要的未验证部分。
* [ ]	只有通过记忆质量门槛的信息才写入 .ai/。
* [ ]	没有加入无关的清理或重构。
